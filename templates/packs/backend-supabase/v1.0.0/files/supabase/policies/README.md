Row Level Security (RLS) policies go here. Example:

-- Enable RLS
-- alter table items enable row level security;

-- Example policy (adjust per auth model)
-- create policy "read_items" on items for select using (true);


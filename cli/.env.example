# Docflow CLI — .env example (development)

# Where generated projects will be written. Use an absolute path.
DOCFLOW_ROOT="${PWD}/.docflow-sandbox"

# AI provider and model.
# Supported providers: openai | anthropic
AI_PROVIDER=openai
AI_MODEL=gpt-4o-mini

# Optional sampling temperature (float)
AI_TEMPERATURE=0.2

# API key for OpenAI (required when AI_PROVIDER=openai and not in dry-run mode)
OPENAI_API_KEY=sk-REPLACE_ME

# Tip: to load this file into your shell for local dev:
#   set -a; source ./cli/.env; set +a

